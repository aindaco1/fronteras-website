## Getting Started

1. Clone the repo
2. Run `npm install`
3. Run `npm start` to start dev server with hot reload at http://localhost:8080/
4. Run `npm run build` to build for production (outputs to `docs/` directory)

## Deployment

The site is deployed via GitHub Pages from the `docs/` directory. After building, commit and push the `docs/` folder.