---
layout: layouts/base.njk
---

{% include "components/header.njk" %}
</br>
<div class="bigcontainer">
  <div class="full-width-post">
    <div class="hero-with-laurel">
      {% if laurelImage %}
      <img src="{{ laurelImage }}" alt="Award" class="laurel-overlay" decoding="async">
      {% endif %}
      <img src="{{ image }}" alt="{{ title }} film still" class="post-hero installation-hero" fetchpriority="high">
    </div>
    {% if youtubeId %}
    </br>
    <div class="embed-container">
      <iframe src="https://www.youtube.com/embed/{{ youtubeId }}" title="{{ title }}" loading="lazy" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>
    </div>
    {% endif %}
    </br>
    {% if artistStatement %}
    <div class="artist-statement">{% if "<p" in artistStatement %}{{ artistStatement | safe }}{% else %}<p>{{ artistStatement | nl2br | safe }}</p>{% endif %}</div>
    {% endif %}
    {% if authors %}
    {% for author in authors %}
    {% if author.bio %}
    </br>
    <p>{{ author.bio | safe }}</p>
    {% endif %}
    {% endfor %}
    {% endif %}
  </div>
  </br></br>
</div>
